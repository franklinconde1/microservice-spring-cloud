server.port: ${spring.application.name}:${spring.application.instance_id:${randon.value}}
spring:
  application:
    name: user
  cloud:
    config:
      enable: true
      uri: http://localhost:8081